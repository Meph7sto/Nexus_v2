interface:
  display_name: "Nexus Admin"
  short_description: "Manage Nexus admin accounts and settings"
  default_prompt: "Use $nexus-admin to inspect and manage Nexus backend accounts safely."

policy:
  allow_implicit_invocation: true
